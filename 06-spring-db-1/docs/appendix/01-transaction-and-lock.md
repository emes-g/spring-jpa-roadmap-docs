## 개요
- 스프링/JPA 로드맵에서 현재 4개(스프링 입문/기본/MVC 1편, HTTP 웹 기본 지식) 완강하고, '스프링 DB 1편' 수강하고 있는 상태
- 현재 기반 지식으로는 위 4개 강좌에 대한 부분은 커버칠 수 있지만, 아직 DB는 초입부라서 질문이 들어왔을 때 명확히 설명할 수가 없어서 appendix로라도 간단하게 정리해보려고 함
- 현재 계획은 4개의 파트인데, 추가될 가능성이 농후함 (대주제별로 파일을 나눌 것이기 때문에, 사실 본 파일에는 '트랜잭션 및 동시성 제어' 부분만 작성될 것으로 추정)

### 1. 트랜잭션 및 동시성 제어
### 2. DB 성능 최적화 및 인덱스
### 3. 데이터 모델링 및 아키텍처
### 4. JPA 핵심 원리 및 최적화
